type Query {
    integrations(id: String): [Integration!]
    credentials: Credentials
}

type Integration {
    id: String
    name: String
    description: String
    documentation: String
}

type Credentials {
    apiKey: String
}
